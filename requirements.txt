yfinance>=0.2.28
requests>=2.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
dash>=2.11.0
pyarrow>=12.0.0

# ── ML intelligence layer (ml_engine.py) ──────────────────────────────────────
# Models B & C (Random Forest, Isolation Forest):
scikit-learn>=1.5
# Model A (LSTM forecast). NOTE: tensorflow-macos / tensorflow-metal have no
# wheels for Python 3.14, so the LSTM runs on Keras 3 with the PyTorch backend,
# which still gets Apple-Silicon GPU acceleration via torch's Metal (MPS) device.
# ml_engine sets KERAS_BACKEND=torch automatically at import time.
torch>=2.2
keras>=3.5
